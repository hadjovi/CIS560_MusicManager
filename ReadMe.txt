When you download the project follow this steps in order to properly run the program.

Step 1: SQL Data
	Run the script Data.sql located in "./Data"
	This will create and populate the tables inside the master localDB
	
	Then run all the procesures script inside "./Data/Procedures"

Step 1: Visual Studio
	Clean the Solution
	Rebuild the Solution
	Run the Program